const prefix = 'AuthContext';

export const LOGIN = `${prefix}/LOGIN`;
export const LOGOUT = `${prefix}/LOGOUT`;
export const SET_LOADING = `${prefix}/SET_LOADING`;
export const SET_LOADED = `${prefix}/SET_LOADED`;